# admin-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: admin-svc
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: {{ .Values.services.admin.port }}
  selector:
    app: admin
