# frontend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: {{ .Values.services.frontend.port }}
  selector:
    app: frontend
